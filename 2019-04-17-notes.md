// ES6 CHANGES
// Lets what you're writing on one line (in a template literal?) automatically continue on the next line.
// Interpolation rather than concatenation.
// Allows default parameters.
// "const" accepts uppercase or lowercase.
// arrow functions

/*    
    const teacher = {
        name: "JimJam",
        speak: function() {
            // Bind a function to specific content:
            let boundFunction = function() { 
                // as an arrow function: let boundFunction = ([any parameters]) => {
                console.log("Later my name is " + this.name);
            }.bind(this); // tells "this", which is 'stuck' within the previous function, that it refers to "teacher"
            // Bound function will always run in bound context.
            setTimeout(boundFunction, 1000);
        }
    };
    teacher.speak()
    */

    // VOCAB: Lexical Binding - binding to the scope where an object is defined, rather than where it is used


// var students = ["Julian", "AJ", "Matt"];
// var x = students [0];
// var y = students [1];
// var z = students [2];
// console.log(x,y,z);

// // ES6 lets you do this:
// let students = ["Julian", "AJ", "Matt"];
// let [x,y,z] = students
// console.log(x,y,z)
// // leaving a blank skips the corresponding student

// let[x, ...rest] = students;
// console.log(x, rest);
// //also returns whole list, but with the first value followed by an array containing the rest
// */

// let completedHomework = () => {
//     return ["Julian", "AJ", "Matt;"]
// }
// let [x,y,z] = completedHomework();
// console.log(x,y,z)
// ...btw we're deconstructing arrays or something
